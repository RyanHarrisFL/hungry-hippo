<h1>Hungry Hippo</h1>


<h2>Add Food for the Poor Hungry Hippo</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Hippo Name:</label>
    <input type="text" id="ca" name="food-name">
  </div>

  <button type="submit">ADD FOOD</button>
</form>


<h2>Choose a food to feed the hungry hippo!</h2>



<h2>Food Eaten</h2>



<div class="row">
  <div class="col-6">

    <ul>
      {{#each hippos}}
      {{#unless devoured}}
      {{> hippos/hippo-block devoured=true}}
      <button class="change-devoured" data-id="{{id}}" data-newfood="{{devoured}}">
        {{#if devoured}}LUNCH TIME!{{else}}EAT ME!{{/if}}
      </button>
      {{/unless}}
      {{/each}}
    </ul>

  </div>

  <div class="col-6">
    <ul>
      {{#each hippos}}
      {{#if devoured}}
      {{> hippos/hippo-block devoured=false}}
      {{/if}}
      {{/each}}
    </ul>

  </div>
</div>
</div>