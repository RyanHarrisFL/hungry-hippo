

<div class="row">
    <div class="col-6">
      <img src="assets/images/hippo-clipart-hippo-outline-7.png"
    </div>

    <div class="col-6">
      <h1 class="text-center mt-5 ">The Very Hungry Hippo</h1>
    </div>
  </div>
</div>


<div class="row">
    <div class="col-3"></div>
    <div class="col-6">
      <p class="text-center">This poor hippo is hungry. Use
the form below to ADD FOOD to his menu. Click on the EAT IT button
next to one of the food items you would like him to eat. </p>
    </div>
    <div class="col-3"></div>
  </div>
</div>

<form class="create-form text-center">

  <div class="form-group">
    <label for="ca">Hippo Name:</label>
    <input type="text" id="ca" name="food-name">
  </div>

  <button type="submit">ADD FOOD</button>
</form>

<div class="text-center">
  <div class="row">
    <div class="col-6">
      <h2>Hugry Hippo Menu</h2>
      <hr>
      <ul>
        {{#each hippos}}
        {{#unless devoured}}
        {{> hippos/hippo-block devoured=true}}
        <button class="change-devoured" data-id="{{id}}" data-newfood="{{devoured}}">
          {{#if devoured}}LUNCH TIME!{{else}}EAT ME!{{/if}}
        </button>
        {{/unless}}
        {{/each}}
      </ul>

    </div>
    <div class="col-6">
      <h2>Digested Food</h2>
      <hr>
      <ul>
        {{#each hippos}}
        {{#if devoured}}
        {{> hippos/hippo-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>

    </div>
  </div>
</div>
</div>